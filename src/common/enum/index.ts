export enum Role {
  USER = "user",
  ADMIN = "admin",
  OWNER = "owner",
}

export type ProductOrderOptions = "latest" | "expensive" | "cheapest";

export enum SortOption {
  LATEST = "latest",
  EXPENSIVE = "expensive",
  CHEAPEST = "cheapest",
}
